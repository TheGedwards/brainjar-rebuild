node_modules
.next
out
.env
.env.local
.DS_Store

# Local secrets / notes — never commit
Supabase DB Password.txt
*password*.txt
*secret*.txt

# Design working files — keep the exported PNGs, not the sources
*.psd
*.tsbuildinfo
next-env.d.ts
